# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.1.0] - 2026-09-05

### Added

- Initial release of Makai, a unified multi-provider AI streaming abstraction layer.
- **Zig core library**: lock-free event queues, type-safe tagged unions, streaming JSON parser, SSE parser, and provider implementations for Anthropic, OpenAI (Completions & Responses), Google (Generative AI & Vertex), Azure OpenAI, AWS Bedrock (stub), and Ollama.
- **Distributed protocol**: client-server wire protocol with envelope-based messaging, partial serialization/reconstruction, and in-process/stdio/SSE/WebSocket transports.
- **Agent loop**: tool execution lifecycle with distributed runtime support.
- **OAuth flows**: GitHub Copilot, Anthropic, Google, and OpenAI Codex OAuth with PKCE.
- **TypeScript SDK**: stdio client transport, high-level APIs for provider completions, streaming, agent runs, auth flows, and model discovery.
- **CLI**: `makai` binary with auth and stdio protocol support.

## Unreleased

### Added

- Added spec §13 "Session Lifecycle & Frame Routing (V1.1)" to [`docs/v1-sdk-agent-provider-spec.md`](docs/v1-sdk-agent-provider-spec.md) — typed identity domains (`session_id` is a correlation/container key, never a resume handle, #198; `in_reply_to` references the request envelope's `message_id` only; `sequence` is per-direction per-session with the exact accepted/echo frame inventory), session lifecycle and ownership (multi-message by design, `.error` persisting after loop-internal failed settlements — provider failures settle via `agent_result` and leave the session `.ready`, `agent_stop` teardown per §6.1, server eviction rights with `agent_not_found` semantics and a normative idle-TTL requirement — in-flight runs exempt — tracked in #202), frame routing (`in_reply_to`-aware delivery planned in #201; until then concurrent runs on one explicit session id are unsupported-by-routing — the server rejects them with `agent_busy`, but the rejection can be misdelivered: the established run can be destroyed or the wrong request can proceed, §13.3.3), admission vs settlement with a single terminal arbiter (server acceptance of `agent_message` — the enqueue — admits; rejected writes return a correlated validation `agent_error` and admit nothing; the `agent_result` frame settles for both `run()` and `stream()` — the SDK projects it into the terminal `agent_end` event; the failure `agent_event`+`agent_error` pair is one settlement; cancellation by `agent_stop` yields no run settlement frame; process exit before settlement is failure — a half-closed client that keeps reading stdout can still receive a delivered settlement, which must not be retried), and the load/resume/replay trichotomy (makai v1 has none; no field implies any). Added [`docs/oap-alignment.md`](docs/oap-alignment.md): the OAP deviations ledger (OAP term → makai construct → status) as the convergence contract for OAP adapter lsm/open-agent-protocol#3, referencing OAP Decision 0001. Server-side gaps the spec marks `[planned]` (envelope/payload session-id agreement, outbound-sequence consistency for echo replies, stopped-id reuse generation/tombstone, EOF/disconnect cancellation, settlement-publication failures, stale `tool_result` correlation) are tracked in #204; the SDK teardown guards in #205 — see `docs/oap-alignment.md` for the authoritative queued-work list.

- Added a cross-compile smoke job to CI that compiles the non-native release targets (`aarch64-macos`, `x86_64-macos`, `aarch64-linux`, `x86_64-windows`) on every PR, so macOS/Windows target regressions surface before a release tag instead of failing the tagged release run (#196).
- Documented the Zig stream/result memory-ownership contract for package consumers in [`docs/zig-stream-memory-ownership.md`](docs/zig-stream-memory-ownership.md): event strings are borrowed by default (owned-event streams — `stream.owns_events == true`, e.g. OpenAI Completions — transfer each polled event to the consumer, which frees it with `deinitAssistantMessageEvent`), `wait()` returning `null` (not a `done` event) is the completion signal, and results are taken via `cloneResult()` before the stream is deinit'd. Linked from the README (#186).
- Added `EventStream.cloneResult(allocator)` on `AssistantMessageStream`: one-call deep copy of the completed result that is fully owned (`is_owned = true`), survives `deinit()`, and is safe to `AssistantMessage.deinit()`. Added `ai_types.cloneToolCall`/`deinitToolCall` for collecting `toolcall_end` tool calls, plus unit tests covering the safe consumer flow (stream → events → result → clean deinit) under `std.testing.allocator` (#186).
- Fixed `cloneAssistantMessage` leaking a partially built content block when a field dupe fails mid-block; `cloneResult()`'s OutOfMemory path is now leak-free (regression-tested with `std.testing.FailingAllocator`) (#186).
- Fixed the OpenAI Completions provider publishing completed results whose `api`/`provider`/`model` strings were borrowed from the thread-owned model freed by `ctx.deinit()` before `complete()` — the result dangled for any consumer reading it (including `cloneResult()`). Metadata is now duped into the result with `is_owned = true`, matching every other built-in provider (found in review of #186).
- Added `npm run check:declarations`: rebuilds the SDK into a clean `dist/`, then verifies the `makai` npm tarball ships `*.d.ts` declarations under `dist/src` (including `dist/src/index.d.ts` and a matching declaration for every shipped `.js`) and that a fresh-install consumer project type-checks cleanly under strict TS with `skipLibCheck` disabled. Wired into CI (`ts-sdk-e2e`) and the release packaging job so a build without declarations can no longer ship (#184).

### Changed

- Upgraded the Zig toolchain requirement from Zig 0.15.2 to Zig 0.16.0.
- Removed the unused `libxev` Zig package declaration; `zig/build.zig.zon` now declares no external Zig package dependencies.
- Routed Makai runtime I/O, HTTP, filesystem, random, stdio, timing, and networking internals through Zig 0.16 `std.Io`-based APIs while preserving Makai-owned wrapper boundaries for future backend evolution.
- Split and documented the expanded Zig unit test matrix used by CI, including the Makai CLI and agent subgroups.

### Fixed

- Fixed the stdio transport routing frames to waiters by `stream_id`/`session_id` only, so two overlapping calls on one consumer-supplied agent session id competed for a single per-session route and misdelivered each other's replies (#201, spec §13.3.1): the duplicate start's `agent_busy` rejection was skip-discarded by the established call's pre-acceptance `in_reply_to` guard (the duplicate then surfaced a full `responseTimeoutMs` timeout), could be consumed by the established call after acceptance (tearing the legitimate session down), and an uncorrelatedly accepted `agent_started` let the duplicate submit its message under a session it did not own. `nextFrameForStream`/`nextFrameForSession` now accept a `correlate` wait option naming an outstanding request's `message_id`: while such a wait is in flight, frames whose `in_reply_to` equals a registered request id are delivered to that request's waiter — promptly, via a poke, even while the waiter is queued behind the transport read lock — and never to a waiter merely sharing the route; frames without `in_reply_to` (or unmatched) keep stream/session-routed behavior, so the single-run fast path is unchanged, and re-routed frames never land in a queue the re-routing waiter itself dequeues from (no busy-spin; SDK-layer re-enqueueing remains a non-fix). The agent SDK correlates every frame wait of an attempt with its `agent_start` `message_id` (`run()` and `stream()`) and rejects a pre-acceptance `agent_started` whose `in_reply_to` names a different request. The pre-existing SDK `in_reply_to` skips remain as defense for unmatched-reply delivery. Spec §13.3.1/§13.3.3 updated to `[current]`.
- Fixed release builds for macOS targets, which failed with `unable to find framework 'Security'. searched paths: none`: Zig skips its native SDK/framework detection when an explicit `-Dtarget=*-macos` is passed. `build.zig` now resolves the macOS SDK (`$SDKROOT` when set, otherwise `xcrun --show-sdk-path` on macOS hosts) and passes it as the build sysroot plus framework search path for the Security/CoreFoundation frameworks; the release workflow exports `SDKROOT` on the macOS jobs (#196).
- Fixed cross-compiling for Windows, which failed with `no field named 'HOME' in struct 'Io.Threaded.Environ.String'`: `compat.getEnvVarOwned(_, "HOME")` now resolves the home directory per target — POSIX keeps the memoized `HOME` environ scan, Windows resolves `USERPROFILE`, then `HOMEDRIVE` ++ `HOMEPATH` (a plain `HOME`, e.g. from Git Bash, still wins as a last resort) (#196).
- Fixed the TS SDK leaking a server-side agent session on every `agent.run`/`agent.stream` call (#199): the server removes a session only on `agent_stop`, which the SDK previously sent only on abort, so every completed or failed run left its session registered for the process lifetime and reusing a consumer-supplied `options.session_id` on the same client failed with `agent_busy`. The SDK now tears the session down on terminal frames (reason `"completed"`, carrying the tracked inbound sequence — the server rejects out-of-order stops) and on error and auth-retry-abandon paths (stop only — a background drain there would consume the frames of an immediate follow-up attempt), awaits a stop-reply-correlated frame drain on terminal and early-consumer-close exits so a follow-up run reusing the id starts clean (including past the trailing `agent_end` the server queues after `agent_result`), never stops a session whose id was rejected as `agent_busy` (it belongs to another live run), and skips stale tail frames from prior attempts on a reused id before its own `agent_start` is accepted. Spec §6.1 now defines session teardown ownership.
- Fixed `MakaiStdioClient.nextFrame` leaving timed-out waiters registered in `frameWaiters` (#199, found in review): `handleLine` resolves waiters FIFO without checking settlement, so a timed-out (already rejected) waiter entry caused the next incoming frame to be delivered to a dead promise and silently dropped — e.g. a follow-up agent run losing its `agent_started` and hanging until `responseTimeoutMs` after a prior run's terminal drain timed out once. Timed-out waiters are now removed from the queue when their timer fires. `nextFrameForSession` additionally accepts an optional `AbortSignal`: an aborted wait re-routes any frame it had dequeued back to the session buffer instead of consuming it, so an abandoned drain read cannot swallow a reused session id's next frame.

### Migration Notes

- See [`docs/zig-0.16.0-downstream-migration.md`](docs/zig-0.16.0-downstream-migration.md) for downstream source migration guidance for Makai Zig consumers.
